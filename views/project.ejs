<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/global.css" />
  <link rel="stylesheet" href="/css/nav.css" />
  <link rel="stylesheet" href="/css/project.css" />
  <link rel="stylesheet" href="/css/splendor.css" />

  <script src="/js/jquery.js"></script>
  <script src="https://cdn.jsdelivr.net/gh/google/code-prettify@master/loader/run_prettify.js"></script>
  <title><%= project.title %></title>
</head>

<body>
  <header><%- include("partial/nav.ejs", {path: path}) %></header>
  <main>
    <section id="poster" style="background-image: url('/data/projects/<%=project.id %>/head-img.png');">
      <div id="poster-cover"></div>
      <h1 id="project-title"><%= project.title %></h1>
      <p><%= project.createdAt < project.updatedAt ? "Edited by " : "Created by " %><%= project.author %> at <%= new Date(project.updatedAt).toLocaleString() %></p>
    </section>
    <section id="project">
      <div id="content" class="markdown">
        <%- project.content %>
        <div class="nav-neighbor">
          <% if (prevProject.length > 0) {%>
          <a href="/project/<%= prevProject[0]._id %>"><%= "Previous: "+ prevProject[0].title %></a>
          <% } %>
          <% if (nextProject.length > 0) {%>

          <a href="/project/<%= nextProject[0]._id %>"><%= "Next: "+ nextProject[0].title %></a>
          <% } %>
        </div>

      </div>

    </section>
    <section id="controls">
      <a onclick="window.history.back()">&lt Go Back</a>
    </section>
  </main>

</html>